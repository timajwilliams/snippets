import { Node } from 'unist';
export declare enum Style {
    Smart = "smart",
    Middle = "middle",
    End = "end"
}
export declare type Props = {
    style?: Style;
    length?: number;
};
export declare type NodeWithChildren = Node & {
    children?: Node[];
    value?: string;
};
export declare type TruncateStyle = {
    [key: string]: (url: string, truncateLen: number, ellipsisChars?: string) => string;
};
export declare function remarkTruncateLinks({ style, length, }?: Props): (ast: NodeWithChildren) => void;
